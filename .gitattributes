*.sh  text eol=lf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Jednorázový externí migrační nástroj (Money S3) — drží se v repu, ale NEpatří
# do produkčních artefaktů (git archive / release bundle). Runtime ho nespouští.
tools/money-s3-import export-ignore
